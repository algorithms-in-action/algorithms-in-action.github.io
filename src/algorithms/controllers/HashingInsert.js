export default {
    initVisualisers() {
    },

    hashInit(tableSize) {
        let table = new Array(tableSize);

        return table;
    },

    hashInsert(table, key) {
    },

    hashDelete(table, key) {
    }

    // run(chunker, { nodes }) {
    // },
}
